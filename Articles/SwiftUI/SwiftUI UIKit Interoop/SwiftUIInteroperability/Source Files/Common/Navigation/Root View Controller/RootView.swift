//
//  RootView.swift
//  SwiftUI Interoperability
//

import SwiftUI

/// An application root view.
/// Will probably never be replaced by appropriate root flow main view before it's become visible.
struct RootView: View {
    var body: some View {
        Text("Root View Controller")
    }
}
